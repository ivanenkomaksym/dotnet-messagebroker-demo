{
    "name": "searchapi",
    "version": "1.0.0",
    "description": "A microservice for searching and recommending products",
    "author": "Bard",
    "license": "MIT",
    "dependencies": {
        "@elastic/elasticsearch": "^7.9.0",
        "elasticsearch": "^16.7.3",
        "express": "^4.18.2",
        "fs": "^0.0.1-security",
        "ts-node": "^10.9.1",
        "typescript": "^4.9.5"
    },
    "scripts": {
        "start": "node ./index.ts",
        "build": "npm run tsc && npm run bundle",
        "test": "npm run build && jest ./src/**/*.test.ts",
        "bundle": "cross-env NODE_ENV=production tsc ./src/**/*.ts -m es6 --outDir ./dist",
        "tsc": "tsc --watch"
    },
    "devDependencies": {
        "@types/express": "^4.17.21"
    }
}
